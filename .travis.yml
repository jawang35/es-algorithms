language: node_js
node_js:
  - 4
  - 6
install:
  - npm install
script:
  - npm run lint && npm run test:coverage
before_install:
  - pip install --user codecov
after_success:
  - codecov --file coverage/coverage.json --disable search
